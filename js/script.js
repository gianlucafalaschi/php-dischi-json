const { createApp } = Vue;

createApp({
    data() {
        return {
            test: 'questo è un test'
        };
    },
    methods: {
        
    },
    mounted() {
        
    }
}).mount('#app');